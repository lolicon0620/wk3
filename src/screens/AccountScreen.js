import React from "react";
import { Text, View } from "react-native";

const AccountScreen = ({ navigation }) => {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>我做了按鈕但，沒內裝</Text>
    </View>
  );
};

export default AccountScreen;
