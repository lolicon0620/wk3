import React from "react";
import { Text, View } from "react-native";

const BookshelfS = ({ navigation }) => {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>I am My Books</Text>
    </View>
  );
};

export default BookshelfS;
